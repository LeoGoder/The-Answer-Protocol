name: Lint C++ (GUI)

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  cpplint:
    runs-on: ubuntu-latest
    steps:
      - name: Retrive code
        uses: actions/checkout@v4

      - name: Python config
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install cpplint
        run: pip install cpplint

      - name: Launch cpplint
        run: cpplint --filter=-legal/copyright --recursive gui/
